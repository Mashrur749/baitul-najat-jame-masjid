node_modules/

# Build output — regenerate with `npm run build` / `npm run assets`
_site/
dist/
.cache/

# Derived from brand/tokens.json by `npm run tokens`. Never hand-edit, never commit.
src/css/tokens.generated.css

# Vendored webfonts — base64, large. Regenerate with `npm run fonts`.
brand/fonts/fonts.css
brand/fonts/*.woff2
brand/fonts/*.ttf

# Posters downloaded from the admins' events sheet at build time (src/_data/activities.js).
src/images/posters/

*.log
.DS_Store
incoming/
